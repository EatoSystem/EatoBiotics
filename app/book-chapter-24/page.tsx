import { createChapterPage, generateChapterMetadata } from "@/components/book/chapter/chapter-page-factory"

export const generateMetadata = generateChapterMetadata(24)
export default createChapterPage(24)
