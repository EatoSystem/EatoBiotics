{
  "$comment": [
    "Which tables actually exist in production. Read by scripts/check-schema-drift.mjs.",
    "",
    "This file exists because supabase/migrations.sql CANNOT answer the question.",
    "Eight tables in daily use — profiles, leads, deep_assessments, journal_entries,",
    "plate_data, referrals, meal_scans, food_intelligence_reports — are live in",
    "production but are never created by that file; they predate it. A guard parsing",
    "CREATE TABLE would flag all eight on day one and the allowlist would swallow the",
    "real signal.",
    "",
    "It also exists because that file's own comments have been wrong before: Migration",
    "41 carried a 'PROPOSED — DO NOT APPLY' header and was applied to production",
    "anyway. A comment is not a record. This is.",
    "",
    "APPLY RITUAL: whoever applies a migration moves its tables from `pending` to",
    "`applied` in the same PR. That is the whole maintenance burden.",
    "",
    "`applied` seeded 2026-08-18 from a read-only information_schema query against",
    "production (ephmojiwlcebenholhpc) — 40 tables."
  ],

  "applied": [
    "ai_usage",
    "analyses",
    "assessment_journeys",
    "cms_audit_log",
    "cms_books",
    "cms_chapter_mirror",
    "cms_chapters",
    "cms_content",
    "cms_content_media",
    "cms_content_tags",
    "cms_content_versions",
    "cms_import_batch",
    "cms_media",
    "cms_tags",
    "consultations",
    "contributions",
    "deep_assessments",
    "email_optouts",
    "email_sends",
    "food_intelligence_reports",
    "food_protocols",
    "glp1_logs",
    "glp1_profile",
    "household_members",
    "journal_entries",
    "leads",
    "meal_plans",
    "meal_scans",
    "monthly_gut_plans",
    "monthly_reviews",
    "plate_data",
    "plate_recipes",
    "profiles",
    "referrals",
    "stability_assessments",
    "stability_logs",
    "stripe_processed_events",
    "subscription_events",
    "twin_state",
    "weekly_checkins"
  ],

  "pending": [
    { "table": "feedback", "migration": 46, "issue": 229 },
    { "table": "reviews", "migration": 45, "issue": 229 }
  ]
}
